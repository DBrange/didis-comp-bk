package services

import "context"

func (s *TournamentService) SearchCompetitorInTournament(ctx context.Context, tournamentID, name string)(any, error){
	// Find all tournament registion with tournamentID

	// Buscar los usuarios o usuarios invitados que esten asociados en tournament registration que contengan, iniciando por el nombre,
	// las letras iniciales que vienen en name

	// Enviar todos justos

	return nil, nil
}